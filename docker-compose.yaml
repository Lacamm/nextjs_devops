services:
  app:
    container_name: app
    image: node:22-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    command: sh -c "sleep infinity" # & npm run dev"

